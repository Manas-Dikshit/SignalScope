interface:
  display_name: "SignalScope"
  short_description: "Repo-specific patterns and workflows for SignalScope"
  default_prompt: "Use the SignalScope repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true